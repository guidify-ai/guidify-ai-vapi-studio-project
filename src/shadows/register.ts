/**
 * Optional private overlays (integrations, destinations).
 * Keep secrets out of git — prefer `src/private/` + `.gitignore` in your fork
 * when you add never-commit modules (see sample / website apps).
 */
export function registerShadows(): void {
  // no-op in the public starter
}
