# Shadows

Place **optional**, app-specific overlays here (or under a gitignored `src/private/`).

The public starter ships a no-op `register.ts`. Product apps use this seam for
CRM hooks, SMS, or destinations that must not land in the open skeleton.
